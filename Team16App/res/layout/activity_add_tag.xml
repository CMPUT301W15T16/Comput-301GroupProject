
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.team16app.TakePhotoActivity"
    android:background="@drawable/bg" >

    <ListView
        android:id="@+id/tagview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_below="@+id/addnewtag" >

    </ListView>

    <EditText
        android:id="@+id/tagname_add"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/tagview"
        android:layout_toLeftOf="@+id/addnewtag"
        android:ems="10"
        android:hint="Tag Name" >

        <requestFocus />
    </EditText>

    <Button
        android:id="@+id/addnewtag"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/tagname_add"
        android:layout_alignParentRight="true"
        android:onClick="Addnewtag"
        android:text="Add Tag" />

</RelativeLayout>
