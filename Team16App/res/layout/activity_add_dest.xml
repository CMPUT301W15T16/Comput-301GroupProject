<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".AddDestActivity"
    android:background="@drawable/bg" >

    <EditText
        android:id="@+id/destName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:ems="10"
        android:hint="Destination Name" >

        <requestFocus />
    </EditText>

    <EditText
        android:id="@+id/reasonDest"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/addnewDest"
        android:layout_alignLeft="@+id/destName"
        android:layout_alignParentRight="true"
        android:layout_below="@+id/destName"
        android:layout_marginTop="34dp"
        android:ems="10"
        android:gravity="center_vertical|left|top"
        android:hint="Reason(s) for travel"
        android:inputType="textMultiLine" />

    <Button
        android:id="@+id/addnewDest"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_marginBottom="22dp"
        android:onClick="saveDest"
        android:text="Finish Adding" />

</RelativeLayout>